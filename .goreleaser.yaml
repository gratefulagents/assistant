version: 2

project_name: assistant

before:
  hooks:
    - go mod tidy

builds:
  - id: assistant
    main: ./cmd/assistant
    binary: assistant
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/gratefulagents/assistant/internal/assistant.buildVersion={{ .Version }}
      - -X github.com/gratefulagents/assistant/internal/assistant.buildCommit={{ .FullCommit }}
      - -X github.com/gratefulagents/assistant/internal/assistant.buildDate={{ .CommitDate }}
    mod_timestamp: "{{ .CommitTimestamp }}"
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - id: assistant
    builds:
      - assistant
    format: binary
    name_template: '{{ .ProjectName }}-{{ .Os }}-{{ .Arch }}{{ if eq .Os "windows" }}.exe{{ end }}'

checksum:
  name_template: SHA256SUMS
  algorithm: sha256

dockers_v2:
  - images:
      - "ghcr.io/gratefulagents/assistant"
    tags:
      - "{{ .Version }}"
      - "latest"
    dockerfile: Dockerfile.goreleaser
    ids:
      - assistant
    platforms:
      - linux/amd64
      - linux/arm64
    labels:
      "org.opencontainers.image.created": "{{ .Date }}"
      "org.opencontainers.image.name": "{{ .ProjectName }}"
      "org.opencontainers.image.revision": "{{ .FullCommit }}"
      "org.opencontainers.image.source": "https://github.com/gratefulagents/assistant"
      "org.opencontainers.image.version": "{{ .Version }}"

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
      - "^style:"
      - "^refactor:"

release:
  mode: keep-existing
  replace_existing_artifacts: true
